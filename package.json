{
  "name": "vishwas-job-portal",
  "version": "1.0.0",
  "description": "A comprehensive job portal with role-based authentication and modern UI",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup-db": "cd server && npm run setup-db"
  },
  "keywords": ["job-portal", "react", "nodejs", "mysql", "authentication"],
  "author": "Vishwas Job Portal Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 